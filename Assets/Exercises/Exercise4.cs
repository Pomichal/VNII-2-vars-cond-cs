using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exercise4 : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        // TODO: This sphere is falling down (because of the RigidBody component). If the GameObject reaches height -10, set its position to (10,  15, 0)
        // transform.position.y to get height and transform.position = new Vector3(10, 15, 0) to set position

    }
}
