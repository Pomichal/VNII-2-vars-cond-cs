using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Exercise5 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // TODO: Create three variables: age (int), has licence (bool), with parent (bool)
        // On Start print out, whether according to these three parameters the person can or cannot drive a car.
        // To evaluate multiple conditions at once use "&&" (AND - all of the inputs must be true to becoma true) or "||" (OR - at least one of the inputs should be true).
        // A person can drive (in Slovakia), if he/she has at least 18 and a driving license or has at least 17, a driving license and is with parent

    }

}
